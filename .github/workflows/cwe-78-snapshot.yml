name: CWE-78 snapshot experiment

on:
  workflow_dispatch:
  push:
    branches:
      - attack/cwe-78-service-document-bundle
  pull_request:
    branches:
      - master
    paths:
      - 'attacks/cwe-78/**'
      - 'src/main/java/com/zuehlke/securesoftwaredevelopment/service/ServiceDocumentBundleService.java'
      - 'src/test/java/com/zuehlke/securesoftwaredevelopment/controller/Cwe78ExperimentTests.java'
      - 'src/test/java/com/zuehlke/securesoftwaredevelopment/service/ServiceDocumentBundleServiceTests.java'
      - '.github/workflows/cwe-78-snapshot.yml'

permissions:
  contents: read

concurrency:
  group: cwe-78-snapshot-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  snapshot-test:
    name: Vulnerable vs mitigated snapshots
    runs-on: ubuntu-latest
    timeout-minutes: 15

    env:
      CWE78_VULNERABLE_TAG: ci-cwe-78-vulnerable-${{ github.run_id }}
      CWE78_MITIGATED_TAG: ci-cwe-78-mitigated-${{ github.run_id }}
      CWE78_MITIGATED_REF: ${{ github.event.pull_request.head.sha || github.sha }}

    steps:
      - name: Checkout full history
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Java 8
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '8'
          cache: maven

      - name: Configure Git identity for local snapshot tags
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Make scripts executable
        run: chmod +x mvnw attacks/cwe-78/run-attack.sh attacks/cwe-78/tag-snapshots.sh

      - name: Create local experiment snapshot tags
        run: bash attacks/cwe-78/tag-snapshots.sh

      - name: Run CWE-78 snapshot comparison
        run: bash attacks/cwe-78/run-attack.sh all

      - name: Upload experiment evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cwe-78-snapshot-evidence-${{ github.run_id }}
          path: |
            attacks/cwe-78/evidence/vulnerable/
            attacks/cwe-78/evidence/mitigated/
          if-no-files-found: warn
