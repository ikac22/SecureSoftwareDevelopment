name: CWE-90 snapshot experiment

on:
  workflow_dispatch:
  push:
    branches:
      - attack/cwe-90-ldap-technician-search
  pull_request:
    branches:
      - master
    paths:
      - 'attacks/cwe-90/**'
      - 'src/main/java/com/zuehlke/securesoftwaredevelopment/service/LdapTechnicianDirectory.java'
      - 'src/test/java/com/zuehlke/securesoftwaredevelopment/Cwe90ExperimentTests.java'
      - 'src/test/java/com/zuehlke/securesoftwaredevelopment/LdapTechnicianSearchIntegrationTests.java'
      - '.github/workflows/cwe-90-snapshot.yml'

permissions:
  contents: read

concurrency:
  group: cwe-90-snapshot-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  snapshot-test:
    name: Vulnerable vs mitigated snapshots
    runs-on: ubuntu-latest
    timeout-minutes: 15

    env:
      CWE90_VULNERABLE_TAG: ci-cwe-90-vulnerable-${{ github.run_id }}
      CWE90_MITIGATED_TAG: ci-cwe-90-mitigated-${{ github.run_id }}
      CWE90_MITIGATED_REF: ${{ github.event.pull_request.head.sha || github.sha }}

    steps:
      - name: Checkout full history
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Java 8
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '8'
          cache: maven

      - name: Configure Git identity for local snapshot tags
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Make scripts executable
        run: chmod +x mvnw attacks/cwe-90/run-attack.sh attacks/cwe-90/tag-snapshots.sh

      - name: Create local experiment snapshot tags
        run: bash attacks/cwe-90/tag-snapshots.sh

      - name: Run CWE-90 snapshot comparison
        run: bash attacks/cwe-90/run-attack.sh all

      - name: Upload experiment evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cwe-90-snapshot-evidence-${{ github.run_id }}
          path: |
            attacks/cwe-90/evidence/vulnerable/
            attacks/cwe-90/evidence/mitigated/
          if-no-files-found: warn
